#include <stdio.h>

/**
 * main - num and alphabet
 * Return: 0
 */

int main(void)
{
	int num;
	char a;

	for (num = 48 ; num < 58 ; num++)
	{
		putchar(num);
	}
	for (a = 'a' ; a <= f ; a++)
	{
		putchar(a);
	}

	return (0);
}
